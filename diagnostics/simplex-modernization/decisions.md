# Implementation decisions

This is the chronological decision record from the sequential implementation.
Historical observations describe their stage of the work; the final integration
report contains the final measurements and default decision. Local validation
artifacts are retained in the worktree for reproduction and crash recovery.

1. - Ruling: The task extractor accepts only `Task N` headings, while the approved plan uses F01–F25 in seven linked files. Extract exact feature briefs into this workspace without changing the approved feature IDs; retain one ledger for the overall plan. Cost if wrong: tooling bookkeeping, not algorithm behavior.

2. F01: Ruling: Update the retry JET test to use the concrete runtime SolveContext type after diagnostic parameterization, and expand it to both Nothing and SimplexDiagnostics observers. The old test passed an unspecified UnionAll context, losing the observer type and generating artificial dynamic-dispatch reports. Four concrete analyses (Float64/Rational{BigInt}, diagnostics off/on) report zero errors without production changes. Cost if wrong: an abstract-caller inference regression could escape this concrete-runtime check; end-to-end typed solve and MOI checks remain in the suite.

3. Ruling: Initial large-case baseline uses a declared 30-second per-solve cap, three repetitions, and one million iterations for both original and current sources. The plan commands with 1800 seconds remain supported. This bounds initial calibration cost while retaining every numerical failure and resource stop. Cost if wrong: failures occurring after the cap are unobserved and require longer F25 validation; capped cases cannot substantiate speedup.

4. Ruling: Record the already completed full-suite invocations in this ledger instead of invoking task-done to repeat them after committing unchanged code; developer instructions prohibit unjustified repeated testing. Cost if wrong: completion depends on the captured logs and checked source hashes rather than a redundant post-commit run.

5. Task 2 Ruling: Interpret MOI reset as a fresh optimizer restoring :legacy; MOI.empty! preserves simplex_strategy like every existing optimizer setting. Existing adapter tests explicitly require empty! to retain options, so resetting only this option would violate that contract. Cost if wrong: callers wanting a reset must construct a new optimizer or set :legacy explicitly.

6. Ruling: Reuse F01 original-source quick data for status/objective/iteration comparison in F02; freshly measure the immediate parent and both F02 profiles under the same runner. Original and parent numerical paths already agree exactly in F01, and F02 enables no algorithm changes. Cost if wrong: no fresh original-revision performance comparison is available; do not claim one.

7. F03 Ruling: dual_step is oriented (reduced cost divided by orientation*row). Flips are rebuilt for each actual candidate; tolerated earlier breakpoints are not automatically flipped. Optional feasible flips supply displacement only when needed. Group capacity skips infeasible early groups; at most max_pivot_candidates full-row validations occur. Finite capacity exhaustion is distinguished from all numerical rejection paths. Exact arithmetic uses zero relaxation. The legacy implementation is unchanged and both normal and refined dual selection dispatch through the configured policy. Full pivot application/recovery remains F04/F06 work.

8. Ruling: Add an independent internal pivot_validation stage switch for F04 instead of enabling all future recovery work through recovery=true. Extend the original switch list and benchmark keys accordingly. Keep legacy behavior by default. Transactional application and validation helpers are isolated in src/simplex_pivot.jl; this is a production-file organization extension to the brief, not a scope change.

9. F07: Ruling: Add a separate feasibility_recovery policy switch, enabled by adaptive, so its ablation preserves F06 basis recovery. Cost if wrong: an extra internal switch to maintain; default legacy remains unchanged.

10. Ruling: F09 uses a separate incremental_primal_pivots switch — disabling the new feature must retain the already committed F08 flip behavior for meaningful ablation — cost if wrong: one additional internal policy field to maintain.

11. Ruling: apply_primal_pivot! requires the ratio test's leaving_state keyword — a zero signed step does not identify which bound the leaving variable reaches — cost if wrong: internal direct callers must supply one extra argument.

12. Ruling: F09 audits may publish a verified feasible correction instead of rejecting every inaccurate prediction — the specification requires full recomputation on drift, and rejecting already verified corrections caused repeated false pivot failures — cost if wrong: extra nonmonotone recovery steps; independent basis/feasibility checks and final original-model certification remain mandatory.

13. Ruling: preserve the pre-existing blend reader failure and its original frozen file; do not modify the parser/input or drop the failed case to force a successful benchmark. Finish the unchanged adaptive holdout profile, preserving both CLI exit1 outcomes and failed input records. Scope/cost: blend remains unavailable to solve, explicitly reported; unrelated parser repair is not part of F11.

14. Ruling: use at most3attempt levels (powers-of-two margin escalation), deterministic8..23 index multipliers, boundedlocalcostscale1..2, total displacement cap512*user dual tolerance, and two-window cooldown after restoration/recovery. Skip fixed/free/basic variables, nonrepresentable steps, or shifts exceeding the cap; exact arithmetic returns0 before floating calculations. These constants derive from the existing8..23 tolerance margin and bounded escalation, not from holdout tuning. New automatic trigger will require F11 monitoring and F07 shared driver; ablations without dependencies retain the old cost-stall fallback.

15. F17 Ruling: UMFPACK documentation's F[:] is unavailable in installedJulia; usepublicF.L/F.U/F.p/F.q/F.Rs. More importantly Rs can be a divisor at extreme scales despite documented multiplication. One public backend solve identifies multiplicative/divisive convention; ambiguity/nonfinite probe returnsnothing for densefallback. No internal pointers. Cost: extra one-time adapter work, measured with extraction; an uncertain adapter is unavailable rather than numerically guessed.

16. F18 Ruling: internal Indexed solve overloads expose kernel_mode=:sparse/:dense/:auto. Auto uses deterministic half-occupancy materialization, including between stages when updates densify support. This is a conservative F18 capability; F19 owns measured-cost selection/hysteresis and canforceper-operationchoices. Costifwrong: intermediateindexedcallschooseaslowercorrectkernel, not changedcertificationoruserdefaults. Dense modebypasseslazyadapterbuild; unavailablebaseadaptersuseoriginalbackendwithsameT.

17. F19 begins from84983c3; parentarchive/tmp/jsimplex-F19-parent-84983c3 verified9d747f6f. Brief/read-ahead read. Mode RED61852 exit1 missingKernelModeState; testswrittenbeforeimplementation. Implementing bounded3-sample median cost state +totalwork +.1/.2 occupancy thresholds/twoindications. Ruling: hypersparse remains opt-in for both strategies pendingF25 rollout; preservingpublicdefaults whileevaluatingnewpipeline. Costifwrong: usersmustexplicitlyenablepotentialbenefituntilvalidation. No pipelineintegration/defaultswitchyet.

18. Ruling: F10 actual factor coefficient count includes lazy owned graph matrices/diagonals/scaling/core. Baseline remains the actual fresh factor before lazy activation, so graph construction consumes real fill headroom (normal identity stays below8x). Retired scratch excluded from fill heuristic; reachable cache bytes reported separately, with shared/borrowed storage caveat. F10 complete attempted-iteration timing remains the economic input, not nested sub-timer sums. Operation timings include lazy graph/support/setup costs; sub-timers are explanatory and overlap. Cost median uses successful complete operations; failed operation time remains in diagnostic totals and whole-attempt timing. No numerical tolerances weakened.

19. Task 21: Ruling: run_phase_one! OPTIMAL denotes verified feasibility of the original-model basis only, and returns no objective/primal result — prevents accidentally reporting an auxiliary feasible point as the original objective optimum — cost if wrong: internal consumers require adapting the phase return contract.

20. Task 21: Ruling: append signed actual basis columns for violated basics, with a single verified slack fallback; disable recursively nested auxiliary construction in shared auxiliary drivers — extends arbitrary recovered bases while bounding recovery — cost if wrong: difficult auxiliary losses of feasibility terminate numerically instead of constructing more auxiliary LPs.

21. Task 21: Ruling: retain NUMERICAL_ERROR for the Float64 contradictory-equality Farkas vector [-1,1] with unbounded structural x: rigorous interval accumulation cannot certify A'y=0; exact rational arithmetic must return INFEASIBLE — preserves the existing original-model proof requirement — cost if wrong: a mathematically infeasible floating model remains inconclusive rather than risking false infeasibility.

22. Task 22: Ruling: precision transfer returns nothing on cancellation and throws invalid-basis/numerical construction errors while preserving cumulative attempted factor counts; a stop keyword extends the planned positional API — provides F23 a typed transition without fabricating a public solution — cost if wrong: internal callers must distinguish cancelled transfers from numerical exceptions.

23. Task 22: Ruling: reset factor/model-dependent stagnation histories and caches during transfer while preserving journal levels and cooldowns — old histories refer to the old scalar/model context and cannot safely be assigned across types — cost if wrong: a fresh observation window may defer a subsequent perturbation.

24. Task23: Ruling: use an internal max_precision_memory_bytes policy allowance (default1GiB) and conservatively include dense Markowitz Schur storage rather than claim a fully sparse arbitrary-precision fallback — the available backends can allocate a dense core; reject before transfer/factor allocation with an explicit reason — cost if wrong: an otherwise solvable large transfer can be refused; this is an allocation estimate, not a whole-process RSS guarantee, and external benchmarks retain the24GiB OS cap.

25. Task23: Ruling: canonical levels extend by powers of two beyond512 only when an explicit ceiling permits; a noncanonical ceiling does not create an extra ad-hoc level — respects stored BigFloat precision without silently lowering it — cost if wrong: unused precision headroom below the next canonical level can yield an explicit numerical failure.

26. Task23: Ruling: keep recovery orchestration in simplex_precision_recovery.jl, while simplex_precision.jl owns typed copying/transfer; reuse DualRunResult{T} with a factor-independent original witness certificate instead of introducing an unused BoostedRunResult — preserves concrete workspace types and public result compatibility — cost if wrong: later consumers must use the explicit witness helper instead of expecting a result-carried workspace.

27. Task23: Ruling: use one explicit cold inference boundary for automatic precision recovery, with a DualRunResult{T} assertion, rather than infer all BigFloat kernels during every ordinary solve — repeated parent/current reproductions and a compiler profile demonstrate a real cold-start deadline regression — cost if wrong: one dynamic call per rare precision recovery and the need to audit this exact site explicitly in JET. Direct typed transfer, precision orchestration and numerical kernels remain strictly checked; all workspace fields retain concrete types. The new JET helper accepts only RuntimeDispatchReport whose innermost Method is the unique precision boundary, rejecting every other report; the three caller tests additionally require exactly one such report and an inferred concrete result type.

28. Ruling: preserve the original finite-activity certificate contract while permitting exact intermediate-overflow cancellation. The F23 exact fallback previously omitted the final original-type representability check, allowing a mathematically unbounded row activity beyond floatmax to escape the prior numerical rejection. Do not change the existing expectations or tolerances. New Float32/Float64 positive/negative and rounded-endpoint regressions fail20/22 on the frozen candidate; the2exact-cancellation cases already pass. Add an exact finite-range guard to the fallback, then reverify focused public entry and complete suites.

29. Ruling: use the frozen analytic oracle3 for generated/scaled-diagonal rather than the conflicting unscaled GLPK objective2. The pre-existing manifest and passing Rational{BigInt} tests already require3; independent Fraction parsing confirms exact powers-of-two equalities forcingx=y=1 andc=(1,2). Preserve the entire raw GLPK attempt in F23-references.json/glpk_attempt and document its rejection. No production/test/runner/input/tolerance change; no numerical solver workload was added alongside measurements. The first four completed groups now pass all original primal errors, available accepted reference objectives, hard-status compatibility, phase counters and source hashes. Final audit strengthened to reject false INFEASIBLE/UNBOUNDED against OPTIMAL references and require reported original primal error<=configured tolerance.

30. F23 Ruling: preserve five completed pre-reboot groups and rerun only interrupted boost-holdout, with fresh warmups and unchanged source/input/runner/policy hashes — OS interruption lost transient records but not complete evidence — cost if wrong: changed host conditions limit timing comparability; explicitly exclude cross-reboot speed ratios. Resume46744 completed/reaped0; monitor14 complete; noownednumericalworkersremain. Fullsuites252876/1113 remainvalidonunchangedsource30e449ae; no redundant complete-suite rerun for reports only.

31. Task 24: Ruling: after wide correction addition, continue from the accepted original-type projection and rehydrate its exact stored values for the next wide residual, rather than retaining an unreturnable wider candidate — acceptance and final certification remain aligned with the public Solution{T} contract, as allowed by F24 read-ahead — cost if wrong: conservative early stagnation can invoke boosting sooner; the ill-conditioned replay indeed still needs boosting. No source change; document this deliberate limitation rather than imply LP-only recovery.

32. Task 25: Ruling: reuse the complete F24 production gate (253612/253612) after proving byte-identical Project.toml/src and all320 production test files; run the entire changed development suite and numerical GLPK command afresh — production code, production tests and dependencies are unchanged, so another122-minute identical suite adds no evidence — cost if wrong: a host-dependent nondeterministic production failure might not recur; new benchmark behavior is covered by full development tests. Any subsequent production/test change invalidates this reuse.

33. Task 25: Ruling: primary performance comparisons use the public profile with diagnostics/kernel timing off, common explicit steepest_edge pricing and no policy overrides; separate single-sample diagnosed observations provide counters only, not speed scores — d93cfd3 has no SimplexDiagnostics and instrumented old/new ratios would confound numerical behavior with measurement overhead — cost if wrong: diagnostic observations are insufficient for performance claims, explicitly prohibited; public timing retains required7/3 repetitions.

34. Task 25: Ruling: use one current-legacy measured record for both F24-parent and final-legacy labels, subject to source/dependency/runner/options/input identity checks — F25 changes only tooling/docs/development tests, and both production archives hash to cc14d2e3 with identical Manifest bytes — cost if wrong: no independent replicate exists for these equivalent labels; disclose shared evidence and invalidate it if production/options change.

35. Task 25: Ruling: retain prior F19 sparse_large60s limits for pilot87/cba/aaa/fast0507/nw04; only runtime/medium/greenbea use extended360s — original F25 controller unnecessarily expanded every large-case budget, while the plan specifically calls for extended runtime/medium/greenbea checks and prior sparse_large used60s — cost if wrong: other cases that need60–360s remain explicitly censored. No affected large case or holdout has run; this is budget alignment, not outcome-based tuning. All repetition counts unchanged. Paused only scheduler133100 after12records, let kb2 adaptive child finish, verified zombie child exit0 and its4warmups/14OPT samples, preserved record13, then terminated only scheduler. Original script/design snapshots retained. Common worker, source, options of all completed records remain identical.

36. Task 25: First scaling fix run24pass/2fail; interpreted probe shows remaining primal cases have the legitimate legacy Phase I expansion from2to4 structural columns. Ruling: use <=rows and the opposite objective, preserving the analytic objective and nonunit factors but giving primal a feasible zero start — the regression must test the scaling guard, not demand an unimplemented auxiliary projection — cost if wrong: expanded-model dual mapping remains unavailable by design. Probe confirms original2x2 workspace for both algorithms and senses. Preserve failed/probe logs; rerun corrected regression against restored original worker to obtain an unambiguous RED, then restore division fix.

37. Final: Ruling: retain the persistent validation workspace and publish the exhaustive decision record as a linked report rather than deleting the only raw logs, source archives, recovery snapshots and Julia depot — the BSOD recovery and reproducibility requirements make these durable evidence, not disposable scratch; the user requested a separate worktree and no integration — cost if wrong: approximately 530 MiB of local storage remains until the user chooses cleanup. The final response will highlight the consequential decisions and link the full English record.

38. Final: Ruling: leave the pre-existing blend parser failure outside this simplex fix pass — input parsing is separately recorded as unavailable and no solved-LP claim depends on it — cost if wrong: blend remains unusable through the native reader until a dedicated parser fix.

39. Final: Ruling: preserve documented scaled-unit tolerance semantics rather than redesign scaling during final fixes — the specification excludes wholesale scaling changes, and the review found no separate false-proof defect in new conversion/correction certification — cost if wrong: callers must still interpret scaled working tolerances and unavailable original KKT diagnostics as documented; this does not promise all optima have finite strict original-unit certificates.
