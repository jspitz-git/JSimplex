# Reversible LP Presolve Implementation Plan

> **For agentic workers:** Implement inline in this worktree. Use test-driven development for each task.

**Goal:** Reduce simple LP structure and finish optimal solves on the original model from a restored basis.

**Architecture:** `presolve.jl` owns safe reductions and index maps. The simplex run returns an optional basis. `solver.jl` orchestrates presolve, scaling, postsolve, and original-model cleanup.

**Tech Stack:** Julia 1.13, SparseArrays, Test.

**Spec:** `docs/superpowers/specs/2026-09-17-presolve-design.md`

## Global Constraints

- Preserve `Solution{T}` and original-model feasibility checks.
- Respect one shared time and iteration budget.
- Preserve stored BigFloat values and skip inexact floating reductions.
- Keep the public input model unchanged.

---

### Task 1: Reversible reductions

**Files:** Create `src/presolve.jl`; modify `src/JSimplex.jl`, `src/transformations.jl`; create `test/presolve_tests.jl` and include it from `test/runtests.jl`.

**Interfaces:** `presolve_problem(::LinearProblem{T})`; `postsolve_primal(::PresolveResult{T}, x)`; `restore_basis(::PresolveResult, ::Basis)`.

- [x] Write tests for fixed columns, finite-bound empty columns, empty rows, infeasibility, precision skip, and model immutability.
- [x] Run focused tests and observe the missing presolve behavior.
- [x] Implement mapping and safe arithmetic in `src/presolve.jl`.
- [x] Run focused tests and the transformation tests.

### Task 2: Carry a final basis out of simplex

**Files:** Modify `src/dual_simplex.jl`, `src/primal_simplex.jl`; extend `test/presolve_tests.jl`.

**Interfaces:** `DualRunResult{T}.basis::Union{Nothing,Basis}`; retain the existing six-argument constructor for current callers.

- [x] Test that an optimal dual and primal run supplies an original-column basis.
- [x] Run tests and observe missing basis data.
- [x] Copy the optimal basis into the run result; map primal phase-I artificial columns back to row activities where possible.
- [x] Run focused tests.

### Task 3: Original-model cleanup

**Files:** Modify `src/solver.jl`, `src/presolve.jl`; extend `test/presolve_tests.jl`.

**Interfaces:** `cleanup_original(problem, restored_basis, options, context, iterations, refactorizations)` returns a `DualRunResult{T}`.

- [x] Test an optimal reduced model needing original-model recomputation, and a zero iteration budget.
- [x] Run tests and observe absence of cleanup.
- [x] Integrate presolve into `solve`, restore basis after postsolve, and run cleanup on original continuous problem.
- [x] Run focused tests, then the full suite.

### Task 4: Documentation and final checks

**Files:** Modify `README.md` and `src/solver.jl` docstring.

- [x] Document supported reductions and cleanup behavior.
- [x] Run the full suite with `JULIA_DEPOT_PATH=/tmp/jsimplex-depot:/home/jspitz/.julia JULIA_PKG_OFFLINE=true julia --project=. test/runtests.jl`.
- [x] Check `git diff --check` and review the final diff.
